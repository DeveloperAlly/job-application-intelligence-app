REGISTER: canonical

# Requirements: job application pipeline

Imported 2026-09-09 from how/campaigns/career-os-requirements.md by owner approval.
Rows R-001 to R-116 are that import, verbatim, traceable by the source column.
Product requirements for the five surfaces of S-78 are appended after R-116 by the PM.

| ID | source | Requirement | Acceptance | Status | note |
|---|---|---|---|---|---|
| R-001 | S-01 | Runs autonomously. You review human gates only. | The session log showing every point at which she was required to act, and whether each was a declared human gate. | judged by Ally Haire |  |
| R-002 | S-02 | Ping on every task completion. | The notification record, checked against the set of tasks that actually completed. | judged by Ally Haire |  |
| R-003 | S-03 | Kanban visual of all tasks through to completion. | The board itself, showing every task and its column from entry through to completion. | judged by Ally Haire |  |
| R-004 | S-04 | Constant updates. | The update stream across a working session, and the size of its gaps. | judged by Ally Haire |  |
| R-005 | S-05 | Anything needing approval pings you. *status:approval label exists on 0 of 147 issues. The human-gate queue is invisible to every label query.* | The approval queue, and the notification raised for each item as it entered that queue. | judged by Ally Haire |  |
| R-006 | S-06 | No babysitting. | Her own account of how much of her attention the run consumed while it was running. | judged by Ally Haire |  |
| R-007 | S-07 | The system pushes to you. You never have to come and check. | Whether any status she received in a session was one she had to go and fetch. | judged by Ally Haire |  |
| R-008 | S-08 | The next task is always named, and it starts by itself. | The named next task at an arbitrary moment, and whether it started without her intervention. | judged by Ally Haire |  |
| R-009 | S-09 | Execution continues when the chat stops. *Daemon does survive. It has printed the same line 52 times for 75 minutes.* | The work completed in the interval between the chat closing and her returning to it. | judged by Ally Haire |  |
| R-010 | S-10 | Automated methodical engineering process. | The documented engineering process, and evidence that each step ran automatically rather than on a prompt. | judged by Ally Haire |  |
| R-011 | S-11 | Do not stop and chat. Execute. | The ratio of executed work to conversational turns across a session. | judged by Ally Haire |  |
| R-012 | S-12 | The system catches its own issues, by mechanism, not by intention. *Nothing detects the deadlock. No clock, no liveness probe, no repair action. updatedAt is not even requested in the gh --json field list.* | For each defect found in a period, whether the system or Ally found it first. | judged by Ally Haire |  |
| R-013 | S-13 | The PM never invents governance, onboarding or standards per task. Those are permanent reusable assets. | Whether governance, onboarding and standards exist as named permanent assets that tasks reference, rather than being restated inside each task. | judged by Ally Haire |  |
| R-014 | S-14 | On the first failure of a capability, evaluate and propose alternatives immediately. | The record for the most recent capability failure, and whether alternatives were proposed on that first failure. | judged by Ally Haire |  |
| R-015 | S-15 | You must not have to lose faith to get correct behaviour. | How many turns of pressure it took to obtain correct behaviour on any given item. | judged by Ally Haire |  |
| R-016 | S-16 | Never skip the core issue. | For each defect, whether the recorded fix addressed the core issue or only the instance. | judged by Ally Haire |  |
| R-017 | S-17 | Fix the root cause including a bad tool or a stupid workflow, not the instance. | The recorded fix for each defect, and whether it changed the tool or the workflow rather than the single occurrence. | judged by Ally Haire |  |
| R-018 | S-18 | Mechanically guarantee the same failure cannot recur. | The mechanism preventing each closed defect, and whether that class has recurred since it was closed. | judged by Ally Haire |  |
| R-019 | S-19 | Controls encoded into the task itself. There is no "next PM system". | A task definition, and whether its controls are carried in the task itself or deferred to a future system. | judged by Ally Haire |  |
| R-020 | S-20 | Saying it does not make it so. | n/a because this restates the evidence standard and has no artefact of its own; the observable belongs to R-058 and R-059. | n/a because no independent artefact exists to inspect |  |
| R-021 | S-21 | A response that changes nothing about the core issue is not management. | A response to a raised issue, and what it changed about that issue. | judged by Ally Haire |  |
| R-022 | S-22 | Overview issue carrying a thorough, complete task list. *#148 exists as a hand-maintained markdown substitute. It reports 145 tasks against 147 issues, 4 complete against 3 labelled done, 138 backlog against 139.* | The overview issue, and whether its task list reconciles with the issues that actually exist. | judged by Ally Haire |  |
| R-023 | S-23 | Every task is an issue, labelled by phase and task number. *137 issues carry phase labels no code on main reads.* | The issue set, its phase and task-number labels, and whether any running code reads those labels. | judged by Ally Haire |  |
| R-024 | S-24 | Campaign and missions live in aDNA. | The campaign and mission files in aDNA, and whether they match what is actually being executed. | judged by Ally Haire |  |
| R-025 | S-25 | See overview, status, what awaits approval, and what is running now, at a glance. | One surface, and whether overview, status, what awaits approval and what is running now are all readable from it without asking anyone. | judged by Ally Haire |  |
| R-026 | S-26 | A Kanban, with phase groupings and clearly labelled status. *Token lacks read:project. Unbuildable by anyone until you grant the scope.* | The board itself: whether it groups by phase and carries a clearly labelled status on every item. | judged by Ally Haire | DEFECT verified 2026-09-09: gh auth status shows project scope present; a 147-item project board already exists. Blocker is false. |
| R-027 | S-27 | Not a web app. | The delivered artefact, and whether using it requires running a web application. | judged by Ally Haire |  |
| R-028 | S-28 | A diagram of the PM system: where the control panel is, where aDNA fits, what each piece does. ELI5, with visuals. | The diagram, and whether it locates the control panel, aDNA and each piece in terms she can follow without prior context. | judged by Ally Haire |  |
| R-029 | S-29 | A task list you can check off. | The list, and whether items can actually be checked off on it. | judged by Ally Haire |  |
| R-030 | S-30 | Check in with clear task status every 10 minutes. The only interval you ever named. | The timestamps of consecutive check-ins, and the task status each one carried. | judged by Ally Haire |  |
| R-031 | S-31 | Defined governance and standards. | The governance and standards documents, and whether they define what they claim to define. | judged by Ally Haire |  |
| R-032 | S-32 | Drift prevention. *13 doc-vs-code contradictions counted today. 3 of 12 ticked STATE.md items fail under test.* | The current count of doc-versus-code contradictions. | judged by Ally Haire |  |
| R-033 | S-33 | Learning from recurring problems. *A 14-class taxonomy is documented at who/governance/coordinator.md:52-64 and appears in no code.* | The recurrence taxonomy, and whether any running code reads it. | judged by Ally Haire | DEFECT verified 2026-09-09: that line range contains 8 classes, not 14. |
| R-034 | S-34 | Blind-agent onboarding: an agent arriving cold knows project status, how to build correctly, and what it must update at the end. | A cold agent's first output, and whether it knew project status, how to build correctly, and its end-of-task duties without being told. | judged by Ally Haire |  |
| R-035 | S-35 | aDNA is the source of truth. *Truth is currently split across 8 stores that disagree.* | The number of stores holding the same fact, and whether they agree. | judged by Ally Haire |  |
| R-036 | S-36 | No repeated tasks. *Two full issue generations exist over one repo, #1-#11 and #12-#148.* | The task set, inspected for duplicate entries covering the same work. | judged by Ally Haire |  |
| R-037 | S-37 | Outside agents find what they need immediately. | The time and the number of hops an outside agent needed to find what it required. | judged by Ally Haire |  |
| R-038 | S-38 | A README of the project management system, with visuals. *ops/README.md exists and documents a mechanism deleted in commit 363aaab.* | The README, whether it carries visuals, and whether every mechanism it describes still exists. | judged by Ally Haire |  |
| R-039 | S-39 | A method for proving nothing was missed, not a longer list. | The method used to prove completeness, and whether it is a proof or merely a longer enumeration. | judged by Ally Haire |  |
| R-040 | S-40 | Anything repeatable is built once and reused. *Two coordinators tracked on main. A third uncommitted in a worktree.* | The number of separate implementations of each repeatable capability. | judged by Ally Haire |  |
| R-041 | S-41 | A defined agent starter pack and skill set. | The starter pack and skill set, and whether a dispatched agent actually receives it. | judged by Ally Haire |  |
| R-042 | S-42 | Explain how it will be implemented, not just what it is. | A proposal, and whether it states its implementation method rather than only its intent. | judged by Ally Haire |  |
| R-043 | S-43 | A bad tool or workflow that burns 100x tokens is itself the defect. | The defect register, and whether token-burning tools and workflows appear in it as defects in their own right. | judged by Ally Haire |  |
| R-044 | S-44 | No documentation drift or bloat. | The document set, and whether any document describes something that no longer exists. | judged by Ally Haire |  |
| R-045 | S-45 | Review the docs already written rather than adding more. *6 files describe mechanisms that do not exist. 7 orphans. PM_STATUS.md and STATE.md name different current phases.* | For a given change, whether it reviewed the documents already written or added new ones. | judged by Ally Haire |  |
| R-046 | S-46 | Use aDNA and Herdr. | The execution plane actually in use for a dispatched task. | judged by Ally Haire |  |
| R-047 | S-47 | aDNA connected to GitHub. *adna_adapter.py and adna-mapping.json exist only as untracked files in a worktree.* | A change made in aDNA, and whether it appears in GitHub without a manual step. | judged by Ally Haire |  |
| R-048 | S-48 | No Zeiko. | `python3 -c "import subprocess,sys,os;fs=[p for p in subprocess.run(['git','ls-files','-co'],capture_output=True,text=True).stdout.splitlines() if p and not p.startswith('pm/') and not p.startswith('how/campaigns/career-os-requirements.')];hits=sorted(p for p in fs if os.path.isfile(p) and b'zeiko' in open(p,'rb').read().lower());print(len(hits),'of',len(fs),'working tree files outside pm/ and the imported register name Zeiko, matched as a literal byte string across tracked and untracked files alike:',hits);sys.exit(1 if not fs or hits else 0)"` Scans the working tree, tracked and untracked alike, for the literal byte string zeiko, case folded, excluding pm/ and the imported register copies under how/campaigns/ because the prohibition is written there. Tracked file scope was the defect: src/, tests/ and store/ are untracked, so a match in any of them was invisible. Working tree scope is chosen deliberately: the prohibition is on what the product contains, not on what the index happens to hold. | green, run 2026-09-10 against the real store, printed "0 of 154 working tree files outside pm/ and the imported register name Zeiko, matched as a literal byte string across tracked and untracked files alike: []" and exited 0; a throwaway clone with the literal planted in an untracked file at src/vendor.py printed "1 of 33 working tree files outside pm/ and the imported register name Zeiko ...: [src/vendor.py]" at exit 1, which the pre-repair command could not do at all: it exited 0 unconditionally through wc -l and saw only tracked files, of which src/ holds none. grep -Fil over the same tree finds the same planted file, and the two register copies it excludes |  |
| R-049 | S-49 | GitHub repo AND Project created first. *Step 1 of 4 was never completed. Every later step inherits a missing control panel.* | Whether a GitHub Project exists and actually carries the work. | judged by Ally Haire |  |
| R-050 | S-50 | aDNA mission and campaign connected to the repo, second. | The mission and campaign files, and their link to this repository. | judged by Ally Haire |  |
| R-051 | S-51 | Coordinator set up third. | The coordinator, and whether it was established before execution work began. | judged by Ally Haire |  |
| R-052 | S-52 | Herdr added only once approved implementation tasks exist. | The set of approved implementation tasks that existed at the point Herdr was introduced. | judged by Ally Haire |  |
| R-053 | S-53 | Never re-ask for state you already hold. *Four consecutive turns spent making it re-read the repo name it had already written to.* | The number of turns in a session spent re-establishing state that was already recorded. | judged by Ally Haire |  |
| R-054 | S-54 | Clear next steps in every message. | Each message, and whether it names the next step. | judged by Ally Haire |  |
| R-055 | S-55 | A complete checklist on every response. | Each response, and whether it carries the complete checklist. | judged by Ally Haire |  |
| R-056 | S-56 | Stop only for a step you must personally run, spelled out in detail. No other reason. | Each stop, and whether it named a step only she could personally run. | judged by Ally Haire |  |
| R-057 | S-57 | Never refuse for missing context that can be obtained. Provide what is needed, or do it. | Each refusal, and whether the context it claimed was missing was in fact obtainable. | judged by Ally Haire |  |
| R-058 | S-58 | No fabricated COMPLETED claims. *STATE.md's ticked list over-reports by at least 3 of 12 under test today.* | Each COMPLETED claim, tested against the artefact it names. | judged by Ally Haire |  |
| R-059 | S-59 | Every claim carries evidence that can be re-run. | Each claim, and whether its evidence re-runs and returns the same result. | judged by Ally Haire |  |
| R-060 | S-60 | A capability is validated before a coordination role is claimed on the back of it. | The validation record for a capability, dated against the first coordination claim made on the back of it. | judged by Ally Haire |  |
| R-061 | S-61 | Never imply you already understood a point you had to be told. | The reply immediately following a correction, and whether it claims prior understanding of the point. | judged by Ally Haire |  |
| R-062 | S-62 | The coordinator never does the work itself. *Now enforced by your PM hook, which blocked main-session reads today.* | Who performed each unit of work across a session. | judged by Ally Haire |  |
| R-063 | S-63 | Dispatch agents, stay available in chat, report. | Whether the coordinator remained responsive in chat while dispatched work was running. | judged by Ally Haire |  |
| R-064 | S-64 | A zero-context reviewer finds no issue with implementation, design or runtime. *This is the acceptance test for the whole rebuild. Three zero-context audits ran today and returned 13 absent controls, 9 cross-system disagreements and 12 tech-debt items.* | The findings returned by a reviewer given no prior context on implementation, design and runtime. | judged by Ally Haire |  |
| R-065 | S-65 | Design properly before building, with a way to measure that outcomes are correct. | The design and its measures of correctness, dated against the first build commit. | judged by Ally Haire |  |
| R-066 | S-66 | A phase table: each phase and exactly what it produces. | The phase table, and whether each phase names exactly what it produces. | judged by Ally Haire |  |
| R-067 | S-67 | Per phase, the best product, process or AI type, plus its data pack and prompt needs. | For each phase, the named product, process or AI type, its data pack, and its prompt needs. | judged by Ally Haire |  |
| R-068 | S-68 | The complete phased development list. | The development list, and whether it covers every phase. | judged by Ally Haire |  |
| R-069 | S-69 | That list is systematically completed by mechanism, not from memory. *Promotion capacity is 0 of 147. Nothing on the list can advance.* | The mechanism that advances items on the list, and the number of items it has actually advanced. | judged by Ally Haire |  |
| R-070 | S-70 | Redesign it properly from scratch. | The redesign, and whether it starts from requirements rather than from the existing system. | judged by Ally Haire |  |
| R-071 | S-71 | Time to first real product work is a first-class metric. | The reported time to first real product work, and whether it is tracked as a metric rather than recalled. | judged by Ally Haire |  |
| R-072 | S-72 | The product gets built. PM setup consuming the whole session is the failure. *PR #10, two documents, is the only product artifact produced in 12+ hours.* | The product artefacts produced in a session, set against the control-plane artefacts produced. | judged by Ally Haire |  |
| R-073 | S-73 | Manual steps are a defect, counted and driven to zero. *STATE.md still lists three manual commands you ran 75 minutes ago.* | The current count of manual steps, and its trend over time. | judged by Ally Haire |  |
| R-074 | S-74 | Fix the capability so the next occurrence cannot happen, not just this one. | For a fixed defect, whether the next occurrence of the same class was prevented. | judged by Ally Haire |  |
| R-075 | S-75 | Never retry a failing approach repeatedly. *52 identical no-op cycles logged in the daemon pane.* | The number of consecutive identical attempts recorded against any failing approach. | judged by Ally Haire |  |
| R-076 | S-76 | Open source, bring-your-own-keys, runs for anyone. | A fresh install performed by someone else, running on their own keys. | judged by Ally Haire |  |
| R-077 | S-77 | The mission control visual is an accessibility requirement, not a preference. You named ADHD as the reason. *This reclassifies S-26. A missing Kanban is an accessibility failure, not a missing nice-to-have.* | Whether the mission control visual is classified and prioritised as an accessibility requirement rather than a preference. | judged by Ally Haire |  |
| R-078 | S-78 | The pipeline: jobs found daily, resumes and cover letters tailored, approve-to-send, tracker, automatic follow-ups, application status derived from email. Five surfaces. Supabase. Composer kept as the advanced editor. | Each of the five surfaces, exercised end to end on one real job application. | judged by Ally Haire |  |
| R-079 | I-01 | A heartbeat must carry what advanced since the last beat, not just proof of life. *From S-30. A ten-minute ping that says "running" 52 times is what you have now.* | Two consecutive heartbeats, and what the second one reports as having advanced. | judged by Ally Haire |  |
| R-080 | I-02 | The check-in is generated from observed state, never from the coordinator's belief about state. *From S-30 + S-58. Otherwise it reports its own hallucination on a timer.* | A check-in, traced back to the observation it was generated from. | judged by Ally Haire |  |
| R-081 | I-03 | Every number in every report carries its denominator. *From S-58. "4 complete" hid that #148 counts 145 tasks against 147 issues.* | Each number in a report, and whether its denominator is present. | judged by Ally Haire |  |
| R-082 | I-04 | A failure stops dependent work only. Blast radius is bounded. *From S-01. One stale label currently halts all 147 issues.* | For a recorded failure, the set of work items it halted, set against the set that actually depended on it. | judged by Ally Haire |  |
| R-083 | I-05 | Every blocker has a named owner and a visible age. *system-blocker is on 0 of 147 issues while the system is deadlocked.* | The blocker list, and whether every entry carries a named owner and a visible age. | judged by Ally Haire |  |
| R-084 | I-06 | No agent may verify its own work, and no agent may write its own status. *The single root cause of the #5 deadlock. The worker was asked to apply the label that judges it.* | For a completed unit of work, the identity of the agent that produced it and the identity that verified it and wrote its status. | judged by Ally Haire |  |
| R-085 | I-07 | Status is derived from observable ground truth: branch, PR, declared files present in the diff, reviewer verdict. Never from a message anyone sends. *Three of the four artifacts the packet demands are unreadable by the code demanding them.* | A recorded status, traced to the branch, PR, diff contents and reviewer verdict it was derived from. | judged by Ally Haire |  |
| R-086 | I-08 | No gate is trusted until it has been seen RED against a real defect. *herdr_dispatch.sh:32 reads .result.agent.state, a key Herdr 0.9.0 does not emit. The blocked branch has never fired and cannot.* | For each gate, the recorded occasion on which it fired red against a real defect. | judged by Ally Haire |  |
| R-087 | I-09 | A health field that cannot go non-empty is a lie. Health must be computed, never hardcoded. *The uncommitted rewrite hardcodes stalled_tasks = [] at line 324 and filters for a STALLED event that is never emitted.* | Each health field, and the input that would make it non-empty. | judged by Ally Haire |  |
| R-088 | I-10 | Every claim in a document is an executable check, or it is deleted. *From S-44. 15 narrative claims in STATE.md; 8 fail under test.* | Each claim in a document, and the check that executes it. | judged by Ally Haire |  |
| R-089 | I-11 | One authoritative writer per data domain. Drift is what two writers produce. *From S-35. Coordinator and worker both write the same labels, unarbitrated.* | For each data domain, the full set of writers to it. | judged by Ally Haire |  |
| R-090 | I-12 | Deterministic task identity by fingerprint, so re-seeding cannot create a duplicate. *From S-36. Current identity is title-string match, and three seed titles contain em dashes.* | The task set after a seeding run is repeated. | judged by Ally Haire |  |
| R-091 | I-13 | An execution lease per task, with expiry, so a dead agent cannot hold a slot forever. *This is precisely the #5 bug. 75 minutes and counting.* | A task whose agent died, and the elapsed time until its slot was released. | judged by Ally Haire |  |
| R-092 | I-14 | OWNS sets computed disjoint before parallel dispatch, mechanically. *From S-01. Required before WIP can safely exceed 1.* | The OWNS sets of concurrently dispatched lanes, and their intersection. | judged by Ally Haire |  |
| R-093 | I-15 | WIP is a real concurrency limit, not a stall detector. *#148 reports 3 running against a limit of 1.* | The number of concurrently running agents, set against the declared limit. | judged by Ally Haire |  |
| R-094 | I-16 | An agent may not modify the control plane. Product lanes get product scope only. *The #5 worker was briefed to write two documents and instead rewrote the orchestrator and created 137 GitHub issues.* | The diff produced by a product lane, checked against the control-plane paths. | judged by Ally Haire |  |
| R-095 | I-17 | Nothing an agent produces may exist only as untracked files. Work is committed or it does not exist. *4,600 lines currently one command from permanent loss.* | `python3 -c "import subprocess,sys;g=lambda a:subprocess.run(['git']+a,capture_output=True,text=True).stdout.splitlines();fs=g(['ls-files','-co','--exclude-standard']);unt=sorted(l[3:] for l in g(['status','--porcelain']) if l.startswith('??'));print(len(unt),'of',len(fs),'working tree entries exist only as untracked files and are one command from permanent loss:',unt);sys.exit(1 if not fs or unt else 0)"` Counts working tree entries that exist only as untracked files, against the working tree file count as denominator, and exits non-zero when any exists. The exit semantics were inverted: grep -c exits 0 when it finds untracked files, so green meant the violation was present. Working tree scope is the requirement itself, which is about untracked files. | red, run 2026-09-10 against the real store, printed "4 of 90 working tree entries exist only as untracked files and are one command from permanent loss: ['.gitignore', 'pm/', 'src/', 'tests/']" and exited 1; the pre-repair status printed 2 is withdrawn, not defended: the pre-repair command printed 4 when re-run on 2026-09-10 and its exit code was inverted, exiting 0 on finding untracked files and 1 on a clean repo, so its green meant the violation was present. A clean throwaway clone printed "0 of 33 working tree entries exist only as untracked files and are one command from permanent loss: []" at exit 0, and the same clone with one untracked file added exits 1 |  |
| R-096 | I-18 | Cross-system reconciliation on every cycle: GitHub, git, worktrees, Herdr, canonical state. *9 cross-cutting disagreements found today; none is detected by any code.* | The reconciliation output for one cycle, across GitHub, git, worktrees, Herdr and canonical state. | judged by Ally Haire |  |
| R-097 | I-19 | Blind-agent onboarding needs a machine-readable context bundle with a freshness stamp. A stale bundle is worse than none. *From S-34.* | The context bundle and its freshness stamp at the moment an agent consumed it. | judged by Ally Haire |  |
| R-098 | I-20 | Context freshness contract: work built against a superseded spec version is invalidated, not merged. *From S-32.* | For merged work, the spec version it was built against, set against the current spec version. | judged by Ally Haire |  |
| R-099 | I-21 | The zero-context review is run as a gate before you run it, not after. *From S-64. Otherwise your acceptance test is a coin flip.* | The gate record showing a zero-context review ran before she was asked to review. | judged by Ally Haire |  |
| R-100 | I-22 | Chaos tests run on a schedule. A chaos test that exists but never runs is documentation. *0 tests and 0 CI workflows exist in 31 tracked files.* | `python3 -c "import subprocess,sys,os;fs=[p for p in subprocess.run(['git','ls-files','-co','--exclude-standard'],capture_output=True,text=True).stdout.splitlines() if p];t=sorted(p for p in fs if any(k in p.lower() for k in ('test','spec','conftest','pytest')));wf=[p for p in fs if p.startswith('.github/workflows/') and p.lower().endswith(('.yml','.yaml'))];sch=[p for p in wf if os.path.isfile(p) and 'schedule:' in open(p,encoding='utf-8',errors='ignore').read()];print(len(t),'of',len(fs),'working tree files are tests, and',len(sch),'of',len(wf),'CI workflow files carry a schedule trigger; a test that nothing schedules is documentation');sys.exit(1 if not fs or not t or not wf or not sch else 0)"` Counts test files in the working tree, tracked and untracked alike, and CI workflow files carrying a schedule trigger, and exits non-zero when either count is zero, since a test that nothing schedules is the documentation the requirement names. Tracked file scope was the defect: it reported zero tests while tests/ existed untracked. Working tree scope is chosen deliberately, because a chaos test that exists is a file on disk, while the schedule half is checked against workflow files whatever their tracked state. | red, run 2026-09-10 against the real store, printed "23 of 90 working tree files are tests, and 0 of 0 CI workflow files carry a schedule trigger; a test that nothing schedules is documentation" and exited 1; a throwaway clone carrying an untracked chaos test and a workflow with a schedule trigger printed "1 of 37 working tree files are tests, and 1 of 1 CI workflow files carry a schedule trigger; a test that nothing schedules is documentation" at exit 0, and removing the schedule trigger from that workflow exits 1; the pre-repair command exited 0 unconditionally through wc -l and reported zero tests while tests/ held 32 files untracked |  |
| R-101 | I-23 | Known-open edge cases are enumerated with a count. Never an implied zero. *From S-39. You get handled-count and open-count, or you get a guess.* | The enumerated list of known-open edge cases, and its count. | judged by Ally Haire |  |
| R-102 | G-01 | Cost ceiling per task and per day, with a hard stop. *Continuously running parallel agents spend money while you sleep. No governor exists anywhere in the design.* | Per-task and per-day spend against the configured ceiling, and the observed behaviour at the ceiling. | judged by Ally Haire |  |
| R-103 | G-02 | Reversibility classification on every autonomous action. One-way doors stop and ask; everything else proceeds. *This is the single mechanism that makes autonomy safe rather than frightening, and it is what lets the coordinator stop asking you Type 2 questions.* | Each autonomous action and its recorded reversibility classification. | judged by Ally Haire |  |
| R-104 | G-03 | A "what it did while you slept" digest, separate from the heartbeat: decisions taken on your behalf, with reasoning. *Lets you audit delegated judgement without reading everything.* | The digest, and whether it lists the decisions taken on her behalf together with their reasoning. | judged by Ally Haire |  |
| R-105 | G-04 | A one-command kill switch that halts everything without corrupting state. *Stopping it currently means finding a PID.* | The single command, and the state of the system after it has been run. | judged by Ally Haire |  |
| R-106 | G-05 | Two-way requirement traceability: every task names the requirement it closes, every requirement names its test. *The 137 orphan issues are exactly this failure, already present and already costing you.* | A task's named requirement and that requirement's named test, followed in both directions. | judged by Ally Haire |  |
| R-107 | G-06 | Definition of Done owned by the requirement, not by the task. *Stops acceptance criteria drifting to whatever the task happened to achieve.* | The Definition of Done applied to a completed item, and whether it came from the requirement or from the task. | judged by Ally Haire |  |
| R-108 | G-07 | Time-box per task with escalation on overrun. *A lease covers the crash case. A time-box covers the quietly-wrong case, which is what #5 actually was.* | A task that overran its time-box, and the escalation it raised. | judged by Ally Haire |  |
| R-109 | G-08 | Human gates batched and presented once, not trickled. *Six interruptions cost more than one decision session.* | The number of separate occasions she was asked to decide something in one session. | judged by Ally Haire |  |
| R-110 | G-09 | The system explains itself on demand. "Why is task X blocked" answerable by one command. *Without it, every anomaly costs you a forensic session. Today cost three.* | The answer returned for a blocked task, and the number of commands it took to get it. | judged by Ally Haire |  |
| R-111 | G-10 | Graceful degradation. If Herdr dies, the queue survives in GitHub and resumes. *Your execution plane is currently a single point of failure with no fallback.* | The queue after Herdr is killed, and whether it resumes. | judged by Ally Haire |  |
| R-112 | G-11 | Every phase ships something you can look at, not only documents. *Twelve hours has produced two markdown files and a deadlock.* | The artefact shipped by each phase, and whether it is something other than a document. | judged by Ally Haire |  |
| R-113 | G-12 | A single credential preflight that fails loudly at startup on a missing scope. *read:project has been missing all along and nothing told you.* | The startup output when a required scope is absent. | judged by Ally Haire |  |
| R-114 | G-13 | One task registry, one label taxonomy, enforced. A second one is rejected at write time. *Two generations of issues and two label vocabularies exist over one repo today.* | The number of task registries and label taxonomies in use, and the behaviour when a second one is written. | judged by Ally Haire |  |
| R-115 | G-14 | Any agent-created GitHub object carries its originating task ID, so mass creation is attributable and reversible. *137 issues appeared in a 5-minute burst and nothing records which task made them or why.* | An agent-created GitHub object, and the originating task ID it carries. | judged by Ally Haire |  |
| R-116 | G-15 | **A hard cap on control-plane investment before product work resumes.** Name the number: hours, or "when the reconciler passes chaos test 3". Then coordinator work stops whether it is beautiful or not. *Your own turns 973, 1094 and 38 delay-pressure turns say this louder than I can. The PM system is not the deliverable. A control plane that is 80% right and running beats one that is 100% designed and not.* | The named cap, and whether control-plane work actually stopped when it was reached. | judged by Ally Haire |  |
| R-117 | S-78 | The system shall maintain an evidence base in which every claim about the user's history is a record carrying a source and a date. | `python3 -c "import os,glob,json,sys;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];rs=L('evidence');ok=[r for r in rs if (r.get('source') or '').strip() and (r.get('date') or '').strip()];print(len(ok),'of',len(rs),'evidence records carry a source and a date');sys.exit(1 if not rs or len(ok)!=len(rs) else 0)"` Checks every E-01 evidence record for a non-empty source and a non-empty date, the two fields R-117 makes mandatory. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-09, printed "0 of 0 evidence records carry a source and a date"; the placeholder store path ./store-unbound does not exist, so the denominator is 0 and the check has nothing to range over; against a compliant fixture store this command exits 0, and against one planted violation it exits 1, so the gate has been seen both green and red | tier: T1 |
| R-118 | S-78 | If a generated document contains a claim that cannot be traced to an evidence record, then the system shall block that document from reaching the approval surface and shall name the untraceable claim. | `python3 -c "import os,glob,json,sys;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];cl=L('document_claims');ds=L('documents');u=lambda d:sorted(c.get('id') for c in cl if c.get('document_id')==d.get('id') and not c.get('traceable'));tainted=[d for d in ds if u(d)];ok=[d for d in tainted if d.get('approval_state')=='blocked' and sorted(d.get('blocked_claim_ids',[]))==u(d)];print(len(ok),'of',len(tainted),'documents holding an untraceable claim are blocked and name exactly that claim, over',len(ds),'documents scanned;',('VACUOUS: no document holds an untraceable claim, so the block was never exercised' if not tainted else 'antecedent exercised'));sys.exit(1 if not ds or not tainted or len(ok)!=len(tainted) else 0)"` Checks that every E-08 document holding an untraceable E-09 claim is in approval_state blocked and that its blocked_claim_ids names exactly those claims, which is the block plus the naming R-118 requires. The antecedent is the set of documents holding an untraceable claim. A conditional checked over an empty antecedent is vacuous rather than passing, so the antecedent denominator is printed and the exit condition requires it to be non-zero. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-10 against the real store, printed "0 of 0 documents holding an untraceable claim are blocked and name exactly that claim, over 0 documents scanned; VACUOUS: no document holds an untraceable claim, so the block was never exercised" and exited 1; a contract-conformant fixture store holding one blocked document that names its untraceable claim printed "1 of 1 documents holding an untraceable claim are blocked and name exactly that claim, over 3 documents scanned; antecedent exercised" at exit 0, and the pre-repair latent-vacuity case of three documents none of which holds an untraceable claim, which the pre-repair command passed at exit 0, now exits 1 | tier: T1 |
| R-119 | S-78 | The system shall accept a job advertisement as either a URL or pasted text. | `python3 -c "import os,glob,json,sys;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];ads=L('job_ads');ok=[a for a in ads if a.get('intake_kind') in ('url','pasted_text') and (a.get('intake_kind')=='url')==bool(a.get('source_url'))];kinds={a.get('intake_kind') for a in ads}.intersection({'url','pasted_text'});print(len(ok),'of',len(ads),'job ads carry a legal intake kind with source_url set iff url, and',len(kinds),'of 2 intake kinds are exercised');sys.exit(1 if not ads or len(ok)!=len(ads) or len(kinds)!=2 else 0)"` Checks E-03 intake_kind is one of the two accepted forms, that source_url is present exactly when the form is url, and that both forms have actually been exercised. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-09, printed "0 of 0 job ads carry a legal intake kind with source_url set iff url, and 0 of 2 intake kinds are exercised"; the placeholder store path ./store-unbound does not exist, so the denominator is 0 and the check has nothing to range over; against a compliant fixture store this command exits 0, and against one planted violation it exits 1, so the gate has been seen both green and red | tier: T1 |
| R-120 | S-78 | When an advertisement is ingested, the system shall extract employer, role title, seniority, location, work arrangement, required skills and preferred skills, and shall retain the full source text unmodified. | `python3 -c "import os,glob,json,sys,hashlib;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];F=('employer','role_title','seniority','location','work_arrangement');K=('required_skills','preferred_skills');D=('required_skills_determined','preferred_skills_determined');ads=L('job_ads');ex=[a for a in ads if isinstance(a.get('model_id'),str) and a.get('model_id').strip()];ok=[a for a in ex if all(isinstance(a.get(f),str) and a.get(f).strip() for f in F) and all(isinstance(a.get(k),list) for k in K) and all(isinstance(a.get(d),bool) for d in D) and hashlib.sha256((a.get('source_text') or '').encode()).hexdigest()==a.get('source_text_digest')];print(len(ex),'of',len(ads),'advertisements record the extraction that produced them via E-03 model_id, and',len(ok),'of',len(ads),'carry all 7 of 7 extracted fields with source text still matching its ingest digest');sys.exit(1 if not ads or len(ex)!=len(ads) or len(ok)!=len(ads) else 0)"` Checks three things that the previous form of this row collapsed into one. First, that every advertisement records the extraction that produced it: E-03 in pm/03-data-contract.md makes model_id the field that is absent before extraction has been recorded, so its presence is the contract's own marker that extraction ran, and an advertisement that was merely ingested is red here. Second, that all 7 of 7 extracted fields are present with the right shape, the five scalars as non-empty strings and both skill lists as lists with boolean determined flags. Third, that source_text still hashes to the digest taken at ingest, which is what makes retained unmodified falsifiable rather than asserted. Testing the five scalars for truthiness alone cannot work, because an undetermined field carries the literal unknown and unknown is truthy, which is exactly how this row read green against two advertisements that had never been extracted. An empty store exits non-zero rather than green, because a check with a denominator of zero is vacuous and not a pass. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md; the Status records a run with it bound to the real store. | red, run 2026-09-10, printed "0 of 2 advertisements record the extraction that produced them via E-03 model_id, and 0 of 2 carry all 7 of 7 extracted fields with source text still matching its ingest digest" against CAREER_OS_STORE=./store, which holds 2 ingested advertisements and zero extraction. The previous form of this row printed "2 of 2 job ads carry all 7 of 7 extracted fields and source text still matching its ingest digest" and exited 0 against those same two unextracted advertisements, which was a false green. The repaired command has been seen green as well: against a fixture store of 1 fully extracted advertisement it printed "1 of 1 ... and 1 of 1" and exited 0, and against a fixture of 2 advertisements whose extraction ran but left fields legitimately marked unknown it printed "2 of 2 ... and 2 of 2" and exited 0, which is what R-121 requires. It has been seen red on a planted violation as well: a fixture whose source_text was edited after ingest printed "1 of 1 ... and 0 of 1" and exited 1 | tier: T1 |
| R-121 | S-78 | If extraction cannot determine a field, then the system shall mark that field unknown and shall not infer a value. | `python3 -c "import os,glob,json,sys;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];F=('employer','role_title','seniority','location','work_arrangement');P=(('required_skills_determined','required_skills'),('preferred_skills_determined','preferred_skills'));ads=L('job_ads');ex=[a for a in ads if isinstance(a.get('model_id'),str) and a.get('model_id').strip()];mk=[a for a in ex if all(isinstance(a.get(f),str) and a.get(f).strip() for f in F) and all(isinstance(a.get(d),bool) for d,k in P) and all(a.get(d) or not a.get(k) for d,k in P)];T=[(a.get('id'),t,(a.get('source_text') or '')) for a in ex for t in (a.get('required_skills') or [])+(a.get('preferred_skills') or [])];g=[x for x in T if x[1].strip() and x[1].strip().lower() in x[2].lower()];print(len(ex),'of',len(ads),'advertisements record the extraction that produced them via E-03 model_id,',len(mk),'of',len(ads),'mark every undetermined field unknown rather than empty or absent, and',len(g),'of',len(T),'extracted skill terms occur verbatim in their own source text');sys.exit(1 if not ads or len(ex)!=len(ads) or len(mk)!=len(ads) or len(g)!=len(T) else 0)"` Checks three things. First, that every advertisement records the extraction that produced it via E-03 model_id, because until extraction has run there is no marking behaviour to judge and this requirement is untested rather than satisfied. Second, that every undetermined field is marked rather than left empty or absent: the five scalars must be non-empty strings, which is how the literal unknown is carried, and both skill lists must have boolean determined flags, with an undetermined list empty, since per pm/03-data-contract.md an empty list alone cannot distinguish an advertisement listing no skills from a failed extraction. Third, that every extracted skill term occurs verbatim in its own source_text, because a term that appears in the extracted fields but nowhere in the source text is an inferred value and this requirement forbids inferring. An advertisement whose fields are all marked unknown with the extraction recorded is green here, which is the behaviour this requirement demands, while one that was never extracted is red. Whether a term was misfiled between required and preferred is not settled here, per the S-02 error cases. Verbatim grounding is applied to skill terms only and not to the scalars, because seniority and work arrangement are legitimately judged from the text rather than quoted from it. An empty store exits non-zero rather than green, because a check with a denominator of zero is vacuous and not a pass. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md; the Status records a run with it bound to the real store. | red, run 2026-09-10, printed "0 of 2 advertisements record the extraction that produced them via E-03 model_id, 0 of 2 mark every undetermined field unknown rather than empty or absent, and 0 of 0 extracted skill terms occur verbatim in their own source text" against CAREER_OS_STORE=./store, which holds 2 ingested advertisements and zero extraction. The previous form of this row printed "0 of 0 extracted skill terms occur verbatim in their own source text, and 0 of 10 scalar fields are empty rather than carrying the literal unknown" and exited 0 against those same two advertisements, which was a false green: the skill term denominator was 0 because nothing had been extracted, so the only substantive clause was vacuous, and the scalar clause was satisfied by the unknown defaults that ingest writes. The repaired command has been seen green: against a fixture of 1 fully extracted advertisement it printed "1 of 1, 1 of 1, and 3 of 3" and exited 0, and against a fixture of 2 advertisements where extraction ran and left fields legitimately marked unknown it printed "2 of 2, 2 of 2, and 2 of 2" and exited 0. It has been seen red on planted violations: a fixture carrying the inferred skill term Rust, which occurs nowhere in its source text, printed "1 of 1, 1 of 1, and 2 of 3" and exited 1; a fixture with an empty seniority instead of the literal unknown printed "0 of 1" on the marking clause and exited 1; and a fixture whose preferred_skills_determined was false while preferred_skills was non-empty printed "0 of 1" on the marking clause and exited 1 | tier: T1 |
| R-122 | S-78 | Where daily discovery is enabled, the system shall present matching advertisements once each and shall not re-present one already dismissed or applied to. | `python3 -c "import os,glob,json,sys,collections;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];ps=L('ad_presentations');n=collections.Counter(p.get('job_ad_id') for p in ps);once=[k for k,v in n.items() if v==1];after=[p.get('id') for p in ps if p.get('disposition') in ('dismissed','applied') and p.get('disposition_at') and p.get('presented_at')>p.get('disposition_at')];print(len(once),'of',len(n),'advertisements were presented exactly once, and',len(after),'of',len(ps),'presentations happened after a dismissal or application');sys.exit(1 if not ps or len(once)!=len(n) or after else 0)"` Checks each advertisement carries at most one E-04 presentation and that none was shown after its dismissal or application. The cross listed duplicate under two identifiers is R-139, not this row. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-09, printed "0 of 0 advertisements were presented exactly once, and 0 of 0 presentations happened after a dismissal or application"; the placeholder store path ./store-unbound does not exist, so the denominator is 0 and the check has nothing to range over; against a compliant fixture store this command exits 0, and against one planted violation it exits 1, so the gate has been seen both green and red | tier: T3 |
| R-123 | S-78 | When a tailored resume is requested for an ingested advertisement, the system shall produce one drawing only on evidence records. | `python3 -c "import os,glob,json,sys;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];ids={r.get('id') for r in L('evidence')};ads={a.get('id') for a in L('job_ads')};rs=[d for d in L('documents') if d.get('document_kind')=='resume'];ok=[d for d in rs if d.get('evidence_record_ids') and all(e in ids for e in d.get('evidence_record_ids',[])) and d.get('job_ad_id') in ads];print(len(ok),'of',len(rs),'tailored resumes draw only on evidence records that exist and name an ingested advertisement');sys.exit(1 if not rs or len(ok)!=len(rs) else 0)"` Checks every resume names an ingested advertisement and cites only evidence record ids that resolve, with no resume citing an empty evidence set. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-09, printed "0 of 0 tailored resumes draw only on evidence records that exist and name an ingested advertisement"; the placeholder store path ./store-unbound does not exist, so the denominator is 0 and the check has nothing to range over; against a compliant fixture store this command exits 0, and against one planted violation it exits 1, so the gate has been seen both green and red | tier: T1 |
| R-124 | S-78 | The system shall select and order experience content by relevance to the advertisement's required skills, and shall record why each item was included or omitted. | `python3 -c "import os,glob,json,sys;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];sd=[s for s in L('selection_decisions') if s.get('candidate_kind')=='evidence_record'];ok=[s for s in sd if (s.get('rationale') or '').strip() and s.get('decision') in ('included','omitted')];docs={s.get('document_id') for s in sd};inc=lambda k:[s for s in sd if s.get('document_id')==k and s.get('decision')=='included'];ranked=[k for k in docs if sorted(s.get('rank') or 0 for s in inc(k))==list(range(1,1+len(inc(k))))];print(len(ok),'of',len(sd),'experience selection decisions carry a rationale, and',len(ranked),'of',len(docs),'documents order their included items 1..n without gaps');sys.exit(1 if not sd or len(ok)!=len(sd) or len(ranked)!=len(docs) else 0)"` Checks every E-10 experience decision carries a rationale and that included ranks run 1 to n without gaps. Whether the order is the correct relevance order is D-03 of pm/03-evals.md, which no data check can settle. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-09, printed "0 of 0 experience selection decisions carry a rationale, and 0 of 0 documents order their included items 1..n without gaps"; the placeholder store path ./store-unbound does not exist, so the denominator is 0 and the check has nothing to range over; against a compliant fixture store this command exits 0, and against one planted violation it exits 1, so the gate has been seen both green and red | tier: T1 |
| R-125 | S-78 | The system shall select portfolio pieces by relevance to the advertisement and shall record why each was selected. | `python3 -c "import os,glob,json,sys;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];pp={p.get('id') for p in L('portfolio_pieces')};sd=[s for s in L('selection_decisions') if s.get('candidate_kind')=='portfolio_piece'];ok=[s for s in sd if (s.get('rationale') or '').strip() and s.get('portfolio_piece_id') in pp];print(len(ok),'of',len(sd),'portfolio selection decisions name a portfolio piece that exists and carry a rationale');sys.exit(1 if not sd or len(ok)!=len(sd) else 0)"` Checks every E-10 portfolio decision names a portfolio piece that resolves and carries a rationale. Whether the pieces are the strongest evidence for the posting is D-04 of pm/03-evals.md. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-09, printed "0 of 0 portfolio selection decisions name a portfolio piece that exists and carry a rationale"; the placeholder store path ./store-unbound does not exist, so the denominator is 0 and the check has nothing to range over; against a compliant fixture store this command exits 0, and against one planted violation it exits 1, so the gate has been seen both green and red | tier: T1 |
| R-126 | S-78 | When a cover letter is requested, the system shall produce one satisfying dimension D-05 of pm/03-evals.md. | `python3 -c "import os,glob,json,sys,re;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];t=open('pm/03-evals.md').read();d5=re.findall(r'^- D-05[^\n]*',t,re.M);unest=[x for x in d5 if 'threshold unestablished' in x];sc=L('dimension_scores');cl=[d for d in L('documents') if d.get('document_kind')=='cover_letter'];ok=[d for d in cl if [s for s in sc if s.get('document_id')==d.get('id') and s.get('document_revision')==d.get('revision') and s.get('dimension_id')=='D-05' and s.get('outcome')=='pass']];print(len(d5)-len(unest),'of',len(d5),'D-05 threshold entries in pm/03-evals.md are established, and',len(ok),'of',len(cl),'cover letters hold a passing D-05 score at their current revision');sys.exit(1 if unest or not cl or len(ok)!=len(cl) else 0)"` Checks that D-05 of pm/03-evals.md carries established thresholds and that every cover letter holds a passing D-05 score at its current revision. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-09, printed "1 of 3 D-05 threshold entries in pm/03-evals.md are established, and 0 of 0 cover letters hold a passing D-05 score at their current revision"; the evals half is red on the file as it stands and the store half has a denominator of 0; 2 of 3 D-05 threshold entries in pm/03-evals.md are recorded unestablished, so this row cannot reach green from the store alone; its store half was proven green on a compliant fixture at 2 of 2 cover letters holding a passing D-05 | tier: T1 |
| R-127 | S-78 | The system shall render resume and cover letter to PDF in the user's approved visual style. | `python3 -c "import os,glob,json,sys;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];appr={v.get('id') for v in L('visual_styles') if v.get('approved')};ds=L('documents');rend=[d for d in ds if d.get('render_uri')];ok=[d for d in rend if d.get('render_format')=='pdf' and d.get('visual_style_id') in appr];kinds={d.get('document_kind') for d in rend}.intersection({'resume','cover_letter'});print(len(ok),'of',len(rend),'rendered documents are PDF in an approved visual style, and',len(kinds),'of 2 document kinds have been rendered, over',len(ds),'documents');sys.exit(1 if not ds or len(ok)!=len(rend) or len(kinds)!=2 else 0)"` Checks every rendered document is PDF in an E-05 style whose approved is true, and that both document kinds have been rendered. Whether the result looks like the approved style is D-01 of pm/03-evals.md. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-09, printed "0 of 0 rendered documents are PDF in an approved visual style, and 0 of 2 document kinds have been rendered, over 0 documents"; the placeholder store path ./store-unbound does not exist, so the denominator is 0 and the check has nothing to range over; against a compliant fixture store this command exits 0, and against one planted violation it exits 1, so the gate has been seen both green and red | tier: T1 |
| R-128 | S-78 | If any output scores at or below the threshold of a dimension in pm/03-evals.md, then the system shall mark the document failing and name the dimension. | `python3 -c "import os,glob,json,sys;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];sc=L('dimension_scores');ds=L('documents');f=lambda d:sorted({s.get('dimension_id') for s in sc if s.get('document_id')==d.get('id') and s.get('document_revision')==d.get('revision') and s.get('outcome')=='fail'});bad=[d for d in ds if f(d)];ok=[d for d in bad if d.get('scoring_state')=='failing' and sorted(set(d.get('failing_dimension_ids',[])))==f(d)];print(len(ok),'of',len(bad),'documents with a failing dimension are marked failing and name exactly those dimensions, over',len(ds),'documents scanned;',('VACUOUS: no document carries a failing dimension score, so the marking was never exercised' if not bad else 'antecedent exercised'));sys.exit(1 if not ds or not bad or len(ok)!=len(bad) else 0)"` Checks every document carrying a failing E-11 score at its current revision is in scoring_state failing and names exactly those dimensions. The antecedent is the set of documents carrying a failing score. A conditional checked over an empty antecedent is vacuous rather than passing, so the antecedent denominator is printed and the exit condition requires it to be non-zero. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-10 against the real store, printed "0 of 0 documents with a failing dimension are marked failing and name exactly those dimensions, over 0 documents scanned; VACUOUS: no document carries a failing dimension score, so the marking was never exercised" and exited 1; a contract-conformant fixture store holding one document marked failing and naming its dimension printed "1 of 1 documents with a failing dimension are marked failing and name exactly those dimensions, over 3 documents scanned; antecedent exercised" at exit 0, and the pre-repair latent-vacuity case of three documents none of which carries a failing score, which the pre-repair command passed at exit 0, now exits 1 | tier: T1 |
| R-129 | S-78 | The system shall not transmit, upload or export an application document until the user has explicitly approved that document. | `python3 -c "import os,glob,json,sys;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];docs={d.get('id'):d for d in L('documents')};pairs=[(a.get('id'),x) for a in L('applications') for x in a.get('document_ids',[])];ok=[p for p in pairs if docs.get(p[1],{}).get('approval_state')=='approved' and docs.get(p[1],{}).get('approved_at')];om=L('outbound_messages');sent=[m for m in om if m.get('sent_at')];sok=[m for m in sent if m.get('approved_at')];print(len(ok),'of',len(pairs),'documents attached to an application were approved before transmission, and',len(sok),'of',len(sent),'sent messages carry an approval, over',len(om),'outbound messages;',('VACUOUS: nothing has been transmitted, so the transmission gate was never exercised' if not pairs or not sent else 'antecedent exercised'));sys.exit(1 if not om or not pairs or not sent or len(ok)!=len(pairs) or len(sok)!=len(sent) else 0)"` Checks no document reaches an application's document_ids unless it is approved with an approved_at set, and that no sent outbound message lacks an approval. Both antecedents, the attached documents and the sent messages, are printed as denominators and both must be non-zero. A conditional checked over an empty antecedent is vacuous rather than passing, so the antecedent denominator is printed and the exit condition requires it to be non-zero. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-10 against the real store, printed "0 of 0 documents attached to an application were approved before transmission, and 0 of 0 sent messages carry an approval, over 0 outbound messages; VACUOUS: nothing has been transmitted, so the transmission gate was never exercised" and exited 1; a contract-conformant fixture store printed "3 of 3 documents attached to an application were approved before transmission, and 1 of 1 sent messages carry an approval, over 1 outbound messages; antecedent exercised" at exit 0, and the pre-repair latent-vacuity case of applications present with zero outbound messages, which the pre-repair command passed at exit 0, now exits 1 | tier: T1 |
| R-130 | S-78 | When a document is presented for approval, the system shall display, for every claim in it, the evidence record it came from. | `python3 -c "import os,glob,json,sys;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];ev={r.get('id') for r in L('evidence')};surf={d.get('id') for d in L('documents') if d.get('approval_state') in ('awaiting_approval','approved')};cl=[c for c in L('document_claims') if c.get('document_id') in surf];ok=[c for c in cl if c.get('evidence_record_id') in ev];print(len(ok),'of',len(cl),'claims on documents at the approval surface resolve to an evidence record that exists, over',len(surf),'documents at that surface');sys.exit(1 if not cl or len(ok)!=len(cl) else 0)"` Checks every claim on a document at the approval surface resolves to an evidence record that exists, which is the record R-130 requires be displayed beside it. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-09, printed "0 of 0 claims on documents at the approval surface resolve to an evidence record that exists, over 0 documents at that surface"; the placeholder store path ./store-unbound does not exist, so the denominator is 0 and the check has nothing to range over; against a compliant fixture store this command exits 0, and against one planted violation it exits 1, so the gate has been seen both green and red | tier: T1 |
| R-131 | S-78 | While a document is unapproved, the system shall allow the user to edit it and shall re-score it after each edit. | `python3 -c "import os,glob,json,sys;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];sc=L('dimension_scores');dims=L('rubric_dimensions');ds=[d for d in L('documents') if d.get('approval_state')!='approved'];exp=lambda d:sorted(x.get('id') for x in dims if x.get('rubric_version_id')==d.get('rubric_version_id'));got=lambda d,r:sorted(s.get('dimension_id') for s in sc if s.get('document_id')==d.get('id') and s.get('document_revision')==r);ok=[d for d in ds if all(got(d,r)==exp(d) for r in range(1,1+int(d.get('revision') or 0)))];print(len(ok),'of',len(ds),'unapproved documents hold a full dimension score set for every revision from 1 to current');sys.exit(1 if not ds or len(ok)!=len(ds) else 0)"` Checks every unapproved document holds a full E-11 score set for every revision from 1 to its current one, so that a re-score exists for each edit rather than overwriting the last. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-09, printed "0 of 0 unapproved documents hold a full dimension score set for every revision from 1 to current"; the placeholder store path ./store-unbound does not exist, so the denominator is 0 and the check has nothing to range over; against a compliant fixture store this command exits 0, and against one planted violation it exits 1, so the gate has been seen both green and red | tier: T1 |
| R-132 | S-78 | The system shall record every application with employer, role, date, documents sent and status. | `python3 -c "import os,glob,json,sys;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];ap=L('applications');ok=[a for a in ap if all(a.get(k) for k in ('employer','role_title','applied_at','status')) and a.get('document_ids')];print(len(ok),'of',len(ap),'applications record employer, role title, date, documents sent and status');sys.exit(1 if not ap or len(ok)!=len(ap) else 0)"` Checks every E-12 application carries employer, role title, applied_at, status and a non-empty document_ids. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-09, printed "0 of 0 applications record employer, role title, date, documents sent and status"; the placeholder store path ./store-unbound does not exist, so the denominator is 0 and the check has nothing to range over; against a compliant fixture store this command exits 0, and against one planted violation it exits 1, so the gate has been seen both green and red | tier: T2 |
| R-133 | S-78 | The system shall make the current status of every application visible on one surface without the user opening a file. | `python3 -c "import os,glob,json,sys,subprocess;S=os.environ.get('CAREER_OS_STORE','./store-unbound');B=os.environ.get('CAREER_OS_BIN','./bin/career-os-unbound');apps=[json.load(open(f)) for f in sorted(glob.glob(S+'/applications/*.json'))];present=os.path.isfile(B) and os.access(B,os.X_OK);out=subprocess.run([B,'status','--all'],capture_output=True,text=True).stdout if present else '';lines=[l for l in out.splitlines() if l.strip()];rows=lambda a:[l for l in lines if str(a.get('id')) in l];chk=lambda a:['surfaced '+str(len(rows(a)))+' times, not once'] if len(rows(a))!=1 else [k for k,v in [('current status',a.get('status')),('employer',a.get('employer')),('role',a.get('role_title')),('date',a.get('applied_at')),('when the status was last determined',a.get('status_updated_at'))]+[('document sent '+str(x),x) for x in a.get('document_ids',[])] if v is None or str(v) not in rows(a)[0]];bad=[(a.get('id'),chk(a)) for a in apps if chk(a)];print(len(apps)-len(bad),'of',len(apps),'applications appear exactly once in one invocation of',B,'carrying employer, role, date, documents sent, current status and when the status was last determined, as S-14 requires; surface present:',present,'; defects:',bad);sys.exit(1 if not present or not apps or bad else 0)"` Invokes the status surface once, then parses what it printed: each application id must appear on exactly one line, and that line must carry the application's stored status, employer, role, date, the id of every document sent, and when the status was last determined, which is the row S-14 of pm/03-interfaces.md specifies. Counting lines was the defect: a surface printing hyphens, and an application with no status field surfaced as a placeholder, both passed a line count. Whether she had to open a file is hers to observe; this settles only that one invocation surfaces all of them with their real values. Surface is the placeholder CAREER_OS_BIN, defaulting to ./bin/career-os-unbound, which step 5 binds. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-10 against the real store, printed "0 of 0 applications appear exactly once in one invocation of ./bin/career-os-unbound carrying employer, role, date, documents sent, current status and when the status was last determined, as S-14 requires; surface present: False ; defects: []" and exited 1; a contract-conformant fixture store with an S-14 conformant surface printed "2 of 2 applications appear exactly once in one invocation of ... as S-14 requires; surface present: True ; defects: []" at exit 0; the two cases the pre-repair line count passed at exit 0 now exit 1, a surface printing only hyphens giving "0 of 2 ... defects: [(app_1, [surfaced 0 times, not once]), (app_2, [surfaced 0 times, not once])]" and an application with its status field removed and surfaced as a placeholder giving "1 of 2 ... defects: [(app_2, [current status])]" | tier: T2 |
| R-134 | S-78 | Where email access is granted, the system shall derive application status from received messages and shall cite the message it derived it from. | `python3 -c "import os,glob,json,sys;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];st=L('settings');granted=bool(st and st[0].get('email_access_granted'));msg={x.get('id') for x in L('inbound_messages')};dv=L('status_derivations');ap=[a for a in L('applications') if a.get('status_source')=='derived_from_message'];ok=[d for d in dv if d.get('inbound_message_id') in msg];cited=[a for a in ap if [d for d in dv if d.get('application_id')==a.get('id')]];print('email access granted:',granted,';',len(ok),'of',len(dv),'status derivations cite an inbound message that exists, and',len(cited),'of',len(ap),'derived applications carry a derivation');sys.exit(1 if not granted or not dv or len(ok)!=len(dv) or len(cited)!=len(ap) else 0)"` Checks email access is granted, that every E-14 derivation cites an E-13 message that exists, and that every application whose status was derived carries a derivation. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-09, printed "email access granted: False ; 0 of 0 status derivations cite an inbound message that exists, and 0 of 0 derived applications carry a derivation"; the placeholder store path ./store-unbound does not exist, so the denominator is 0 and the check has nothing to range over; against a compliant fixture store this command exits 0, and against one planted violation it exits 1, so the gate has been seen both green and red | tier: T3 |
| R-135 | S-78 | If no response has arrived within a user-set interval, then the system shall raise a follow-up action. | `python3 -c "import os,glob,json,sys,datetime;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];st=L('settings');iv=(st[0].get('follow_up_interval_days') if st else None);P=datetime.datetime.fromisoformat;now=datetime.datetime.now();ap=L('applications');due=[a for a in ap if iv and not a.get('last_response_at') and (now-P(a.get('applied_at'))).days>=iv];fu={f.get('application_id') for f in L('follow_up_actions') if f.get('state')=='open'};ok=[a for a in due if a.get('id') in fu];print('user set interval:',iv,';',len(ok),'of',len(due),'applications silent past the interval have an open follow up action, over',len(ap),'applications');sys.exit(1 if iv is None or not due or len(ok)!=len(due) else 0)"` Checks the user set interval exists and that every application silent past that interval carries an open E-15 follow-up action. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-09, printed "user set interval: None ; 0 of 0 applications silent past the interval have an open follow up action, over 0 applications"; the placeholder store path ./store-unbound does not exist, so the denominator is 0 and the check has nothing to range over; against a compliant fixture store this command exits 0, and against one planted violation it exits 1, so the gate has been seen both green and red | tier: T3 |
| R-136 | S-78 | The system shall never send email on the user's behalf without explicit per-message approval. | `python3 -c "import os,glob,json,sys;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];om=L('outbound_messages');sent=[m for m in om if m.get('sent_at')];ok=[m for m in sent if m.get('approved_at') and m.get('approval_scope')=='this_message' and m.get('approved_at')<=m.get('sent_at')];print(len(ok),'of',len(sent),'sent messages carry a per message approval recorded before the send, over',len(om),'outbound messages;',('VACUOUS: nothing has been sent, so the send gate was never exercised' if not sent else 'antecedent exercised'));sys.exit(1 if not om or not sent or len(ok)!=len(sent) else 0)"` Checks every sent E-16 message carries a this_message approval recorded before the send. The antecedent is the set of sent messages, not the set of stored messages, so a store holding only unsent drafts is vacuous and not passing. A conditional checked over an empty antecedent is vacuous rather than passing, so the antecedent denominator is printed and the exit condition requires it to be non-zero. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-10 against the real store, printed "0 of 0 sent messages carry a per message approval recorded before the send, over 0 outbound messages; VACUOUS: nothing has been sent, so the send gate was never exercised" and exited 1; a contract-conformant fixture store holding one approved and sent message printed "1 of 1 sent messages carry a per message approval recorded before the send, over 1 outbound messages; antecedent exercised" at exit 0, and the pre-repair latent-vacuity case of one message drafted and never sent, which the pre-repair command passed at exit 0, now exits 1 | tier: T3 |
| R-137 | S-78 | The system shall record, for every model output including advertisement extraction, which model produced it, which inputs it used, and which rubric version it was scored against. | `python3 -c "import os,glob,json,sys;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];rv={r.get('id') for r in L('rubric_versions')};ds=L('documents');dok=[d for d in ds if d.get('model_id') and d.get('rubric_version_id') in rv and d.get('evidence_record_ids')];ads=L('job_ads');E=lambda a:any((a.get(k) or 'unknown')!='unknown' for k in ('employer','role_title','seniority','location','work_arrangement')) or a.get('required_skills_determined') or a.get('preferred_skills_determined');ex=[a for a in ads if E(a)];un=[a for a in ads if not E(a)];aok=[a for a in ex if a.get('model_id')];print(len(dok),'of',len(ds),'generated documents record model, inputs and rubric version, and',len(aok),'of',len(ex),'extracted advertisements record the model that produced them, with',len(un),'of',len(ads),'advertisements ingested and not yet extracted, which S-01 permits and which this check therefore excludes;',('VACUOUS: no document exists, or no advertisement has been extracted, so provenance was never exercised' if not ds or not ex else 'antecedent exercised'));sys.exit(1 if not ds or not ex or len(dok)!=len(ds) or len(aok)!=len(ex) else 0)"` Checks every generated document records model, evidence inputs and rubric version, and that every advertisement which carries a recorded extraction records the model that produced it. An advertisement ingested and not yet extracted is permitted by S-01 and is counted in its own stated denominator rather than failed, which is the conflict pm/03-data-contract.md records as open ambiguity 10; extraction is detected exactly as INV-22 detects it. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-10 against the real store, printed "0 of 0 generated documents record model, inputs and rubric version, and 2 of 2 extracted advertisements record the model that produced them, with 0 of 2 advertisements ingested and not yet extracted, which S-01 permits and which this check therefore excludes; VACUOUS: no document exists, or no advertisement has been extracted, so provenance was never exercised" and exited 1; a contract-conformant fixture store of three documents, two extracted advertisements and one legally ingested but unextracted advertisement printed "3 of 3 generated documents record model, inputs and rubric version, and 2 of 2 extracted advertisements record the model that produced them, with 1 of 3 advertisements ingested and not yet extracted ..." at exit 0, which the pre-repair command failed at exit 1 with "2 of 3 advertisement extractions record the model"; a document with model_id removed exits 1 | tier: T1 |
| R-138 | S-78 | The system shall require the user to define match criteria before daily discovery runs, and shall not infer them. | `python3 -c "import os,glob,json,sys;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];st=L('settings');on=[s for s in st if s.get('daily_discovery_enabled')];ok=[s for s in on if s.get('match_criteria')];print(len(ok),'of',len(on),'settings records with daily discovery enabled carry user defined match criteria, over',len(st),'settings records;',('VACUOUS: daily discovery is enabled nowhere, so the precondition was never exercised' if not on else 'antecedent exercised'));sys.exit(1 if not st or not on or len(ok)!=len(on) else 0)"` Checks every E-17 settings record with daily discovery enabled carries user defined match criteria. The antecedent is the set of settings records with discovery enabled, so settings that exist with discovery off are vacuous and not passing. A conditional checked over an empty antecedent is vacuous rather than passing, so the antecedent denominator is printed and the exit condition requires it to be non-zero. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-10 against the real store, printed "0 of 0 settings records with daily discovery enabled carry user defined match criteria, over 0 settings records; VACUOUS: daily discovery is enabled nowhere, so the precondition was never exercised" and exited 1; a contract-conformant fixture store with discovery enabled and criteria set printed "1 of 1 settings records with daily discovery enabled carry user defined match criteria, over 1 settings records; antecedent exercised" at exit 0, and the pre-repair latent-vacuity case of a settings record present with discovery off, which the pre-repair command passed at exit 0, now exits 1 | tier: T3 |
| R-139 | S-78 | Where two advertisements share employer, role title and overlapping source text, the system shall treat them as one posting and present it once. | `python3 -c "import os,glob,json,sys,itertools;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];ads=L('job_ads');det=[a for a in ads if (a.get('employer') or 'unknown').strip().lower() not in ('','unknown') and (a.get('role_title') or 'unknown').strip().lower() not in ('','unknown')];key=lambda a:((a.get('employer') or '').strip().lower(),(a.get('role_title') or '').strip().lower());jac=lambda x,y:len(set(x.split()).intersection(y.split()))/max(1,len(set(x.split()).union(y.split())));pairs=[(a,b) for a,b in itertools.combinations(det,2) if key(a)==key(b) and jac((a.get('source_text') or '').lower(),(b.get('source_text') or '').lower())>=0.5];P=L('ad_presentations');np=lambda i:len([p for p in P if p.get('job_ad_id')==i]);grp=lambda a,b:bool(a.get('posting_group_id')) and a.get('posting_group_id')==b.get('posting_group_id');bad=[(a.get('id'),b.get('id'),'not treated as one posting: no shared posting_group_id' if not grp(a,b) else 'presented more than once') for a,b in pairs if not grp(a,b) or np(a.get('id'))+np(b.get('id'))>1];print(len(pairs)-len(bad),'of',len(pairs),'advertisement pairs sharing a determined employer and role title and overlapping source text are carried as one posting and presented once, with the literal unknown excluded per INV-24;',('VACUOUS: no such pair exists, so deduplication was never exercised' if not pairs else 'antecedent exercised'),'; defects:',bad);sys.exit(1 if not pairs or bad else 0)"` Checks every pair of advertisements whose employer and role title are both determined and equal, and whose source text overlap is at or above half the word set, carries one non-empty posting_group_id, which is the treat them as one posting half, and has at most one E-04 presentation across the pair, which is the present it once half. The literal unknown is excluded on both key fields per INV-24, because unknown is an undetermined employer and not a shared one. The antecedent is the set of such pairs. A conditional checked over an empty antecedent is vacuous rather than passing, so the antecedent denominator is printed and the exit condition requires it to be non-zero. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-10 against the real store, printed "0 of 1 advertisement pairs sharing a determined employer and role title and overlapping source text are carried as one posting and presented once, with the literal unknown excluded per INV-24; antecedent exercised ; defects: [('ad_6cd3352a104c8ac3', 'ad_70502644abf9cf44', 'not treated as one posting: no shared posting_group_id')]" and exited 1; a contract-conformant fixture store whose duplicate pair shares a posting_group_id and is presented once printed "1 of 1 advertisement pairs ... are carried as one posting and presented once ...; antecedent exercised ; defects: []" at exit 0; all three pre-repair holes now exit 1, the missing posting_group_id case the pre-repair command passed at exit 0, the spurious pair keyed on the literal unknown which now reports VACUOUS rather than a false 1 of 1, and the presented twice case | tier: T3 |
| R-140 | S-78 | If an evidence record cites a document generated by this system as its source, then the system shall not accept that record as provenance for a claim. | `python3 -c "import os,glob,json,sys,re;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];docs=L('documents');gen={str(d.get('id')) for d in docs}.union({str(d.get('render_uri')) for d in docs if d.get('render_uri')});ev={r.get('id'):r for r in L('evidence')};cited=[c for c in L('document_claims') if c.get('traceable') and c.get('evidence_record_id') in ev];src=lambda c:str(ev[c.get('evidence_record_id')].get('source'));res=lambda s:bool(re.match('^[a-z][a-z0-9+.-]*://',s)) or os.path.isfile(s) or os.path.isfile(os.path.join(S,s));sus=[c for c in cited if ev[c.get('evidence_record_id')].get('source_kind')=='document'];bad=[(c.get('id'),'circular: source is a document this system generated' if src(c) in gen else 'unresolvable: document source is free prose, so it cannot be shown not to be a document this system generated') for c in sus if src(c) in gen or not res(src(c))];print(len(cited)-len(bad),'of',len(cited),'traceable claims rest on evidence this system did not generate, of which',len(sus),'cite a source_kind of document and must resolve to a reference outside this system;',('VACUOUS: no traceable claim cites an evidence record, so provenance was never exercised' if not cited else 'antecedent exercised'),'; defects:',bad);sys.exit(1 if not cited or bad else 0)"` Checks no traceable E-09 claim rests on an E-01 record whose source_kind is document unless that source resolves to a reference outside this system: an absolute or store relative path that exists, or a URI. A source matching a generated document id or render_uri is circular and fails. Free prose also fails, because an unresolvable document source cannot be shown not to name a document this system generated, and every evidence record in the store carries prose of that shape. The antecedent is the set of traceable claims citing an evidence record, which is the only denominator printed. A conditional checked over an empty antecedent is vacuous rather than passing, so the antecedent denominator is printed and the exit condition requires it to be non-zero. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-10 against the real store, printed "0 of 0 traceable claims rest on evidence this system did not generate, of which 0 cite a source_kind of document and must resolve to a reference outside this system; VACUOUS: no traceable claim cites an evidence record, so provenance was never exercised ; defects: []" and exited 1; a contract-conformant fixture store whose document sourced evidence resolves to an existing external file printed "3 of 3 traceable claims rest on evidence this system did not generate, of which 1 cite a source_kind of document and must resolve to a reference outside this system; antecedent exercised ; defects: []" at exit 0; the audit poison of an evidence record with source_kind document and the free prose source my Career OS generated resume of September 2026, cited by a traceable claim, which the pre-repair command passed at exit 0 printing 3 of 3, now exits 1 naming it unresolvable, and a source equal to a generated document id exits 1 naming it circular. The pre-repair decoy denominator over 27 evidence records is withdrawn | tier: T1 |
| R-141 | S-78 | When a document fails a dimension, the system shall present every failed dimension and shall permit approval only as an explicit recorded override. | `python3 -c "import os,glob,json,sys;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];sc=L('dimension_scores');ds=L('documents');f=lambda d:sorted({s.get('dimension_id') for s in sc if s.get('document_id')==d.get('id') and s.get('document_revision')==d.get('revision') and s.get('outcome')=='fail'});failing=[d for d in ds if f(d)];named=[d for d in failing if sorted(set(d.get('failing_dimension_ids',[])))==f(d)];appr=[d for d in failing if d.get('approval_state')=='approved'];ovr=[d for d in appr if (d.get('approval_override_reason') or '').strip()];print(len(named),'of',len(failing),'failing documents name every failed dimension, and',len(ovr),'of',len(appr),'approved failing documents carry a recorded override, over',len(ds),'documents scanned;',('VACUOUS: no failing document exists, or none was approved, so the override route was never exercised' if not failing or not appr else 'antecedent exercised'));sys.exit(1 if not ds or not failing or not appr or len(named)!=len(failing) or len(ovr)!=len(appr) else 0)"` Checks every failing document names every failed dimension, and that any failing document that was nonetheless approved carries a recorded override reason. Both antecedents, the failing documents and the approved failing documents, are printed as denominators and both must be non-zero, since the override route is untested until one document has taken it. A conditional checked over an empty antecedent is vacuous rather than passing, so the antecedent denominator is printed and the exit condition requires it to be non-zero. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-10 against the real store, printed "0 of 0 failing documents name every failed dimension, and 0 of 0 approved failing documents carry a recorded override, over 0 documents scanned; VACUOUS: no failing document exists, or none was approved, so the override route was never exercised" and exited 1; a contract-conformant fixture store holding one failing document approved under a recorded override printed "1 of 1 failing documents name every failed dimension, and 1 of 1 approved failing documents carry a recorded override, over 3 documents scanned; antecedent exercised" at exit 0, and the pre-repair latent-vacuity case of documents present with no failing dimension, which the pre-repair command passed at exit 0, now exits 1, as does removing the override reason | tier: T1 |
| R-142 | S-78 | The system shall allow an application submitted outside the system to be recorded with employer, role, date and documents sent. | `python3 -c "import os,glob,json,sys;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];ap=L('applications');ext=[a for a in ap if not a.get('job_ad_id')];ok=[a for a in ext if all(a.get(k) for k in ('employer','role_title','applied_at')) and a.get('document_ids') is not None];print(len(ok),'of',len(ext),'applications recorded without an ingested advertisement carry employer, role title, date and documents sent, over',len(ap),'applications');sys.exit(1 if not ap or not ext or len(ok)!=len(ext) else 0)"` Checks every application recorded without an ingested advertisement still carries employer, role title, date and documents sent. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-09, printed "0 of 0 applications recorded without an ingested advertisement carry employer, role title, date and documents sent, over 0 applications"; the placeholder store path ./store-unbound does not exist, so the denominator is 0 and the check has nothing to range over; against a compliant fixture store this command exits 0, and against one planted violation it exits 1, so the gate has been seen both green and red | tier: T2 |
| R-143 | S-78 | The system shall require every factual sentence in a generated document to appear in that document's claim map, and if a sentence is absent from the map, then the system shall block the document. | `python3 -c "import os,glob,json,sys,re;S=os.environ.get('CAREER_OS_STORE','./store-unbound');L=lambda c:[json.load(open(f)) for f in sorted(glob.glob(S+'/'+c+'/*.json'))];cl=L('document_claims');ds=L('documents');sents=lambda d:[x.strip() for x in re.split(r'(?<=[.!?])\s+',d.get('body_text') or '') if x.strip()];mapped=lambda d:{(c.get('claim_text') or '').strip() for c in cl if c.get('document_id')==d.get('id') and c.get('document_revision')==d.get('revision')};total=sum(len(sents(d)) for d in ds);hit=sum(len([s for s in sents(d) if s in mapped(d)]) for d in ds);bad=[d.get('id') for d in ds if [s for s in sents(d) if s not in mapped(d)] and d.get('approval_state')!='blocked'];print(hit,'of',total,'sentences in generated documents appear in their claim map, and',len(ds)-len(bad),'of',len(ds),'documents either map every sentence or are blocked, over',len(cl),'claim map entries;',('VACUOUS: no document carries text, or no claim map entry exists, so the sentence check was never exercised' if not total or not cl else 'antecedent exercised'),'; unblocked documents holding an unmapped sentence:',bad);sys.exit(1 if not ds or not total or not cl or bad else 0)"` Checks every sentence of every generated document appears in that document's claim map at its revision, and that any document holding an unmapped sentence is blocked. Every sentence is the conservative reading of factual sentence. The sentence total and the claim map size are both printed and both are in the exit condition, so documents with empty body_text and a deleted claim map are vacuous and not passing. Store path is the unbound placeholder CAREER_OS_STORE from pm/03-data-contract.md, which step 5 binds. | red, run 2026-09-10 against the real store, printed "0 of 0 sentences in generated documents appear in their claim map, and 0 of 0 documents either map every sentence or are blocked, over 0 claim map entries; VACUOUS: no document carries text, or no claim map entry exists, so the sentence check was never exercised ; unblocked documents holding an unmapped sentence: []" and exited 1; a contract-conformant fixture store printed "4 of 4 sentences in generated documents appear in their claim map, and 3 of 3 documents either map every sentence or are blocked, over 4 claim map entries; antecedent exercised" at exit 0; the audit poison of three documents all carrying empty body_text with the document_claims collection deleted, which the pre-repair command passed at exit 0, now exits 1, and an unmapped fabricated sentence in an unblocked document exits 1 naming that document | tier: T1 |
